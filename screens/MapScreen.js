import { StyleSheet, Text, View } from "react-native";
import React from "react";

const MapScreen = () => {
  return (
    <View>
      <Text>It is the map screen you're looking for.</Text>
    </View>
  );
};

export default MapScreen;

const styles = StyleSheet.create({});
